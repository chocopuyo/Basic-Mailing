<%= form_for(@mail_body) do |f| %>
  <% if @mail_body.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mail_body.errors.count, "error") %> prohibited this mail_body from being saved:</h2>

      <ul>
      <% @mail_body.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
